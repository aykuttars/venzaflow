# Deprecated

GitLab stack artık **`docker/gitlab/`** altında (Portainer path: `/home/management/DockerFiles/gitlab/`).

Kullan: [`../docker-compose.yml`](../docker-compose.yml) ve [`../.env.example`](../.env.example).

Bu klasördeki dosyalar referans içindir; yeni deploy için `new/` kullanma.
