appId: com.venzaflow.eimza
# Spaceless slug → /opt/eimza; brand namespace via /opt/Venzaflow/eimza symlink.
productName: eimza
copyright: Copyright © 2026 Aykut Arslantas
directories:
  buildResources: build
  output: release
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  - '!.github/**'
asarUnpack:
  - 'node_modules/graphene-pk11/**'
  - 'node_modules/pkcs11js/**'
extraResources:
  # Optional bundled PKCS#11 libraries (e.g. OpenSC) for zero-install support.
  # Files live in resources/pkcs11/<platform>/ and are copied to Resources/pkcs11/.
  - from: resources/pkcs11
    to: pkcs11
    filter:
      - '**/*'
win:
  executableName: eimza
  target:
    - nsis
  artifactName: venzaflow-eimza-${version}-win-${arch}.${ext}
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  include: build/installer.nsh
  menuCategory: Venzaflow
  artifactName: venzaflow-eimza-${version}-setup-${arch}.${ext}
  shortcutName: Venzaflow e-imza
  uninstallDisplayName: Venzaflow e-imza
  createDesktopShortcut: always
mac:
  category: public.app-category.business
  # Ad-hoc signature ('-') so Apple Silicon shows a bypassable "unverified"
  # warning instead of the unrecoverable "app is damaged" Gatekeeper error.
  identity: '-'
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  target:
    - dmg
    - pkg
  artifactName: venzaflow-eimza-${version}-mac-${arch}.${ext}
  extendInfo:
    CFBundleDisplayName: Venzaflow e-imza
    CFBundleName: eimza
    NSCameraUsageDescription: Application requests access to the device's camera.
    NSMicrophoneUsageDescription: Application requests access to the device's microphone.
  notarize: false
dmg:
  artifactName: venzaflow-eimza-${version}-mac-${arch}.${ext}
  title: Venzaflow e-imza ${version}
pkg:
  installLocation: /Applications/Venzaflow
  allowAnywhere: false
  isRelocatable: false
linux:
  target:
    - AppImage
    - deb
  maintainer: Aykut Arslantas
  vendor: Aykut Arslantas
  synopsis: Venzaflow e-imza masaüstü imza köprüsü
  description: USB e-imza tokenları ile e-reçete, e-arşiv ve e-fatura imzalama uygulaması.
  category: Office
  # executableName drives the binary name, the /usr/bin symlink and the
  # generated desktop Exec. electron-builder 26 forbids a custom desktop Exec.
  executableName: eimza
  desktop:
    entry:
      Name: Venzaflow e-imza
      StartupWMClass: eimza
appImage:
  artifactName: venzaflow-eimza-${version}-linux-${arch}.${ext}
deb:
  artifactName: venzaflow-eimza-${version}-linux-${arch}.${ext}
  afterInstall: scripts/linux-postinst.sh
  depends:
    - libnotify4
    - libsecret-1-0
    - pcscd
    - libpcsclite1
npmRebuild: true
publish:
  provider: generic
  url: https://example.com/eimza-updates
