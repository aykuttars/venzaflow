# Copy to `.env` at repo root for docker-compose / local dev.
# Production targets default to shared 10.0.0.3 infrastructure
# (Postgres + Redis + RabbitMQ).

# --- PostgreSQL ---
POSTGRES_USER=tenancyuser
POSTGRES_PASSWORD=!->ts961ts11ts.*
POSTGRES_DB=tenancysoft
POSTGRES_HOST=10.0.0.3
POSTGRES_PORT=5432

# --- Django ---
DJANGO_SETTINGS_MODULE=config.settings.prod
SECRET_KEY=change-me-in-production-use-long-random-string
DEBUG=false
ALLOWED_HOSTS=localhost,127.0.0.1,backend,proxy,10.0.0.3
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost

# --- JWT (use a long random secret in prod, separate from SECRET_KEY) ---
JWT_SIGNING_KEY=change-me-jwt-signing-please

# --- Optional seed on container start (dev only) ---
RUN_SEED=false

# --- Redis (result backend & caching) ---
REDIS_HOST=10.0.0.3
REDIS_PORT=6379
REDIS_PASSWORD=MO9Ala5M4uITQYXpX3QErNZFuHx1
REDIS_DB=2

# --- RabbitMQ (Celery broker) ---
RABBITMQ_HOST=10.0.0.3
RABBITMQ_PORT=5672
RABBITMQ_USER=aykutt.ars
RABBITMQ_PASS=ayk55577ayk
RABBITMQ_VHOST=/

# Computed Celery URLs (override only if needed):
CELERY_BROKER_URL=amqp://aykutt.ars:ayk55577ayk@10.0.0.3:5672//
CELERY_RESULT_BACKEND=redis://:MO9Ala5M4uITQYXpX3QErNZFuHx1@10.0.0.3:6379/2
