# Local dev against Docker: copy to .env.development.local → EBARCODE_API_BASE_URL=http://localhost:8001
# Packaged Mac/Win builds use production API (see apiClient DEFAULT_BASE_URL).
EBARCODE_API_BASE_URL=https://venzaflow-api.aykut.in
